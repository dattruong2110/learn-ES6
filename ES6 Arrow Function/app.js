const logger = log => {
    console.log(log);
}

logger('Message...');

// const multiply = (a, b) => a * b;
// console.log(multiply(prompt('Enter a:'), prompt('Enter b:')));

// const sum = (a, b) => ({ a: a, b: a});

// console.log(sum(prompt('Enter a:'), prompt('Enter b:')));